
#include <stdio.h>

#include <vfs.h>
#include <vfs_local_fs.h>

extern int vfs_local_fs_open() {

  return 0;
}

extern int vfs_local_fs_close() {

  return 0;
}

extern vfs_file_search_result* vfs_local_fs_get_files(const char* pattern) {

  return NULL;
}
